/**
 * API Slice - VERSION ALLÉGÉE
 * Export minimal pour éviter le code mort
 */

// Export par défaut pour éviter le fichier vide
export const API_SLICE_INFO = {
  name: 'api-slice',
  version: '1.0.0',
  description: 'API Slice RPM-CL - Exports désactivés pour éviter le code mort',
} as const

// Exports commentés - décommentez si nécessaire :
// export { apiSlice }
// export { useGetPartnersQuery, useGetPartnerByIdQuery, useCreatePartnerMutation, useUpdatePartnerMutation, useDeletePartnerMutation, useGetClassificationsQuery }
